---
openjdk_package: openjdk-8-jre
repository:
  key: "http://packages.confluent.io/deb/{{ confluent.version.split('.')[0] }}.{{ confluent.version.split('.')[1] }}/archive.key"
  url: "deb http://packages.confluent.io/deb/{{ confluent.version.split('.')[0] }}.{{ confluent.version.split('.')[1] }} stable main"
base_package_name: "{% if confluent.oss %}confluent-platform-oss-2.11{% else %}confluent-platform-2.11{% endif %}"
package_name: "{{ base_package_name }}{% if not confluent.latest %}={{ confluent.version }}{% endif %}"
